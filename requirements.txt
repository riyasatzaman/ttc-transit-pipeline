# Python deps for ingestion + local dev (Airflow image installs its own deps).
requests==2.32.3
pandas==2.2.2
pyarrow==16.1.0
snowflake-connector-python==3.12.0
python-dotenv[cli]==1.0.1
pytest==8.3.2

# dbt for SQL transformations against Snowflake.
dbt-core==1.7.13
dbt-snowflake==1.7.3
